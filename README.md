# Guitar chords

To install dependencies:

```bash
bun install
```

To run:

```bash
bun run chords <chord>
```

To display left handed chord diagrams, use the `lh` switch:

```bash
bun run chords C --lh
```
